<!DOCTYPE html>
<html>
<head>
    <title>The Jekolizer</title>
    <link href="stylesheets/screen.css" media="screen" rel="stylesheet" type="text/css" />
</head>
<body>
    <div class="container">
        <h1>The Jekolizer</h1>
        <form method="post">
            <input type="text" value="<%= @jekolized_page.url if @jekolized_page %>" name="url" id="url" />
            <input type="text" placeholder="search" name="s1" id="s1" />
            <input type="text" placeholder="replace" name="r1" id="r1" />
            <input type="submit" value="jekolize" id="submit" />
        </form>
        <div class="clear"></div>
        <% if @jekolized_page %>
        <div class="result">
            Your jekolized URL is:
            <a href="/<%= @jekolized_page.token %>">/<%= @jekolized_page.token %></a>
        </div>
        <% end %>
    </div>
</body>  